SUPABASE_URL=https://joarmzswpufrduectjse.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
BACKUP_ROOT=C:\Users\cot00\OneDrive - Bendigo Flying Club\CRM Backups\Bendigo Flying Club Portal
RETENTION_DAYS=45
PAGE_SIZE=1000

# Optional monitoring. Leave blank to disable local alert delivery.
# Supported types: generic, slack, discord, teams
ALERT_WEBHOOK_URL=
ALERT_WEBHOOK_TYPE=generic

# In-app admin backup failure alerts use SUPABASE_URL and
# SUPABASE_SERVICE_ROLE_KEY above to create unread system notifications
# for every admin user. The service role key must never be added to the
# browser app's .env file.
